Áp dụng transaction (cart,order) ok
Validate cho tất cả API ok
Report sử dụng aggregate ok
Sửa lại thông báo ok
Sử dụng Ref ok
Error Handller ok
Sửa lại addCart ok

phân trang ok
sửa lại chức năng order,lấy dữ liệu từ cart ok
sử lý lỗi cho mỗi Validate
 

clean code



baseService {
   create, find....
}
 category.service.js  {
   ...baseService,
  findWith
 }

Controller {try, catch} ok
BaseResponse {   ok
  statusCode,
  data,
  message  
}
BaseError extend Error {
  statusCode,
  errorCode,
  message
}

throw new BaseErrror({statusCode: 400, error: errorList.NoExistUser})

{
  statusCode: 400,
  errorCode: errorList.NoExistUser.errorCode,
}

tach' route

get-get
put-update
delete-delete

CRUD =id

router
baseService,
params

 /api/{components}/{object}/{content}

 /api/user/admin/:id

 /user/me 
 /user/staff/:id
 GET /user/admin {page, limit, status}

ref category_id sang product ok
su73 dung5 === thay == ok
them dieu kien update vd order can user_id ok
cach dat ten
kiem Validate trong model ok
3 bien thi object ok
bien, ham...cach khai bao phai tuong minh
xem lai ve transaction(khong lam dung khi khong can thiet) ok
doc document mongodb